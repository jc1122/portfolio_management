[pytest]
testpaths = tests
pythonpath = src
markers =
    unit: Fast, isolated tests without external I/O.
    integration: Tests exercising multiple components or performing I/O.
    slow: Long-running scenarios exercised in integration suites.
