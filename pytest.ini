[pytest]
testpaths = tests
pythonpath = src
markers =
    unit: fast, isolated tests without external I/O.
    integration: tests that exercise multiple components or require I/O.
    slow: long-running regression or performance scenarios.
