[pytest]
testpaths = tests
pythonpath = src
markers =
    unit: Fast-running unit tests that avoid I/O and external integrations.
    integration: Tests that exercise multi-component flows or require I/O.
